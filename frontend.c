#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <signal.h>

#include "frontend.h"


int main(int argc, char **argv) {

    //setup input verification
    if(argc < 3){
        printf(WRONG_INPUT);
        exit(1);
    }


    return 0;
}